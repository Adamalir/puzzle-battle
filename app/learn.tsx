import { View, Text } from 'react-native';

export default function LearnScreen() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 24 }}>This is the Learn screen</Text>
    </View>
  );
}